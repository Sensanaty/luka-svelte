# lu-ka.me

This is my personal website, built with SvelteKit and hosted on Netlify.

## Working on the Site

`yarn dev` will spin up the Vite server. `yarn build` will build it, and the built site can be viewed locally with `yarn preview` after it has been built.
